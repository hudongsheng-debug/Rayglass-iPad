# Rayglass 支持中心

适用于 Rayglass（Mac）与 Rayglass iPad。

Rayglass 提供本地光学建模、追迹与分析。你可以从内置示例开始，编辑处方、比较结果，并保存工程或导出图表与数据。

## 联系支持

开发者/运营者：【待填：公开名称】
支持邮箱：【待填：公开支持邮箱】

报告问题时，请提供 App 版本、系统版本、设备型号、重现步骤，以及预期结果与实际结果。可以附上已移除敏感信息的截图或最小示例工程。请勿发送密码、支付信息或未经授权的保密设计。

## 系统要求

- Mac：macOS 14 或更高版本。
- iPad：iPadOS 17 或更高版本。
- 核心计算无需网络，不需要账号或外部光学设备。查看外部材料来源页面需要网络。

## 语言设置

支持简体中文和英语，跟随系统为 Rayglass 指定的 App 语言；切换后请重新打开应用。未匹配的语言回退英语。已有工程的标题、光学面名称与备注保持原样；新建内置示例使用当前语言。

## 第一个设计

1. 打开侧栏“元件与示例”，载入 N-BK7 / N-F2 胶合透镜。
2. 进入“光学设计”。Mac 在表格和检查器中编辑；iPad 点选光学面卡片进入参数面板。
3. 调整半径、间距或口径，等待追迹完成。
4. 选择“自动调焦”，然后到“点列分析”查看 RMS、视场与波长结果。
5. 使用“保存工程”或“另存工程”保存 .rayglass 文件。

数值长度通常为 mm，波长为 µm，角度为 °；镀膜厚度以 µm 表示。半径为 0 表示平面。请始终以输入项显示的单位为准。

## 材料与元件

在“材料库”搜索玻璃，并应用到所选折射面。iPad 上先展开材料详情。目录使用 156 种 SCHOTT 历史数据快照，记录来源和适用波长范围，不代表实时供货、熔次或温度数据。插入独立元件前，请选择面后为空气的光学面。

## 工程保存与设备间交换

.rayglass 保存光学处方、系统设置及已支持的镀膜/非序列参数。光学面 CSV 只包含部分处方信息，不能替代完整工程。优化变量、公差配方及完整分析历史目前不会全部写入工程，重新打开后需重新设置相关运行配置。

Mac 从工具栏或文件菜单打开/保存；iPad 从右上角“…”菜单选择“打开工程”或“另存工程”。可通过你选择的文件位置手动传递工程，包括系统提供的云盘位置。App 不提供自动跨设备同步。新版新增字段可能无法被旧版完整保留，请在回存前备份原文件。

工作副本自动恢复用于减少意外中断造成的损失，不代替独立工程备份。删除 App、清理容器或设备故障可能导致工作副本丢失。

## 导出结果

可导出光路 PNG、Markdown 分析报告、光学面 CSV，以及相关分析页提供的数据 CSV。Mac 使用“交换”菜单及各模块导出按钮；iPad 使用“…”菜单及模块内导出按钮。某些导出需要先运行对应分析。

## 常见问题

### 为什么光线通过率不是 100%？

光线可能被口径截断、发生全反射或未找到有效交点。检查口径、材料、面顺序、间距及诊断信息。顺序分析的通过率是采样光线比例，不是光学透射功率。

### 为什么衍射分析无法运行？

当前衍射模型要求同轴、无限远、轴上、无反射、空气出射、无截光，且 NA ≤ 0.1。检查面偏心、倾斜、介质和采样设置。增加采样后应比较结果是否趋于稳定。

### iPad 分析页显示不全？

复杂分析页可左右滑动。横屏或扩大窗口可同时显示更多控件；页面内容也可上下滚动。

### 计算时间较长怎么办？

减少采样或公差试验次数，先用较小规模验证参数。运行时可选择“停止”。iPad 进入后台会取消当前计算，并保留工作副本。

### 是否支持 Zemax、CAD 或 Python 脚本？

当前不支持 ZMX/ZOS 工程格式、通用 CAD 导入或 App 内脚本执行。可使用 .rayglass 与 CSV 进行支持范围内的文件交换。

### Mac 与 iPad 是否完全相同？

两端提供同源的计算能力，界面针对平台调整。iPad 材料页暂不提供 Mac 的多材料对比、目录散点图及筛选目录 CSV 导出。两端独立发布，交换文件时请留意版本差异。

## 计算适用范围

顺序模型尚无任意内部光阑的入瞳瞄准。衍射是受限的标量近轴模型。优化为有边界的局部搜索，不保证全局最优。公差采用独立均匀扰动。基础非序列模块不包含通用 CAD、散射或嵌套介质；薄膜为独立无损介质模型，尚未耦合光学面追迹。制造和重要工程决策应结合独立基准与实测验证。

## 隐私

查看隐私政策：【待填：公开隐私政策 URL】。
